
/**
 * Local class for bigram count pairs.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */

class MBigramCount{
    int count;
    String bigram;

    MBigramCount(int count, String bigram) {
         this.count = count;
         this.bigram = bigram;
    }
 }